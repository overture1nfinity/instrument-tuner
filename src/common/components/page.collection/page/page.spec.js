describe('page.view.controller', function() {
    beforeEach(module('common'));

    var $controller, $rootScope;

    beforeEach(inject(function(_$controller_, _$rootScope) {
        $controller = _$controller_;
        $rootScope = _$rootScope;
    }));
});
